# Application NODE_ENV=development PORT=3000 APP_NAME=<%= projectName %> # Database — fill in before
running the app DATABASE_HOST=localhost DATABASE_PORT=5432 DATABASE_USER=postgres
DATABASE_PASSWORD=changeme DATABASE_NAME=<%= projectName %>_dev # Auth — generate a secure random
string for production JWT_SECRET=changeme_use_a_long_random_string_in_production JWT_EXPIRES_IN=7d
